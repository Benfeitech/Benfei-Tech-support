{
  "name": "youtube-mp3-downloader",
  "version": "1.0.0",
  "description": "A simple static YouTube MP3 downloader page.",
  "main": "index.html",
  "scripts": {
    "start": "vercel dev",
    "build": "echo 'No build step required for a static site'",
    "deploy": "vercel --prod"
  }
}
